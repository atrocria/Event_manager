// package service;

// import model.Concert;
// import model.Performance;

// public class EventService {
//     //todo check if the event is full and if logics

//     public void addPerformanceToConcert(Concert concert, Performance p) {
//         if (concert.canFitPerformance(p.getDuration())) {
//             concert.addPerformance(p);
//         } else {
//             throw new IllegalArgumentException("Schedule exceeds event time limit!");
//         }
//     }
// }
